//Exercise 2 - Chapter 6

// Write the definition of the function Perimiter() described in exercise 1. The 2 paramaters represent the
// length and width of a rectangle. Have the function return the perimiter.

unsigned long int Perimiter(unsigned short int length, unsigned short int width)
{
    return (length * 2) + (width * 2);
}