// If in Exercise 2, Shape takes no paramaters, Rectangle takes 2 ( length and width ) , but square only takes one
// (length), show the constructor initialization for Square.


Square::Square(int length)
{
    itsLength = length;
}


